#include <iostream>
#include <string>
using namespace std;

string strings_xor(const string &s, const string &t) {
        string res = "";
        for (int i = 0; i < s.size(); i++) {
                if (s[i] == t[i])      // compare characters, not assign
                        res += '0';          // append
                else
                        res += '1';
        }
        return res;
}

int main() {
        string s, t;
        cin >> s >> t;
        cout << strings_xor(s, t) << endl;
        return 0;
}
